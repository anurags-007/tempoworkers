services:
  - type: web
    name: tempoworkers-backend
    env: node
    rootDir: server
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
      - key: JWT_SECRET
      - key: RAZORPAY_KEY_ID
      - key: RAZORPAY_KEY_SECRET
      - key: EMAIL_USER
      - key: EMAIL_PASS
      - key: SMS_API_KEY
